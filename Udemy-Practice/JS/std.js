const username = " ";
function getName(username) {
    if (username.length >= 10 && username.indexof() != -1) {
        console.log("Valid");
    } else {
        console.log("Invalid");
    }
} getName(username);